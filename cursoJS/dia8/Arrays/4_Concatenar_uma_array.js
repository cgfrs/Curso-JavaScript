let numbers = [5, 6, 7, 8]
let letters = ['a', 'b', 'c', 'd']

all = numbers.concat(letters)
half = all.slice(3, 5) // colocar uma parte da array em outra array

console.log(all)
console.log(half)


